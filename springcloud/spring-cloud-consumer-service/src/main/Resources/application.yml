server:
  port: 5080
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://root:root@127.0.0.1:8761/eureka
  instance:
    prefer-ip-address: true
spring:
  application:
    name: consumer-service
logging:
  level: debug